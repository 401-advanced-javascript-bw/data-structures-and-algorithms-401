'use strict';

const BinaryTree = require('../tree.js');

describe('binary tree with preporder, inorder, and postorder', () => {
  xit('preorder should push to root first', () => {
    const BT = new BinaryTree().preorder(2);
  });
});
