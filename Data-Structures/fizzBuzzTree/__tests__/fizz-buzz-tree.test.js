'use strict';
// const Tree = require('../fizz-buzz-tree');

describe('fizz-buzz-tree', () => {
  xit('should check if the value of the node has the type of number', () => {
    // const traverse = new Tree();
  });
});
